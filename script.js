let currentScreen = document.getElementById("current"),
    previousScreen = document.getElementById("previous"),
    currentNum = "";
    previousNum = "",
    operation = ""
    opSings = ["+", "-", "*", "/"];

let getValue = (element) => {
    
    if (opSings.includes(element.value)) {

    }
}